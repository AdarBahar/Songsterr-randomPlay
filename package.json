{
  "name": "random-favorite-picker",
  "version": "1.4.2",
  "scripts": {
    "build": "webpack --mode production",
    "zip": "cd dist && bestzip ../extension.zip *",
    "compile": "npm run build && npm run zip"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4",
    "bestzip": "^2.2.1",
    "terser-webpack-plugin": "^5.3.9",
    "copy-webpack-plugin": "^11.0.0"
  }
}
