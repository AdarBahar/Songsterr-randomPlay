{
  "name": "random-favorite-picker",
  "version": "1.2.1",
  "scripts": {
    "build": "webpack --mode production",
    "minify-css": "cleancss -o dist/styles.min.css src/*.css",
    "zip": "bestzip extension.zip dist/*",
    "compile": "npm run build && npm run minify-css && npm run zip"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4",
    "clean-css-cli": "^5.6.2",
    "bestzip": "^2.2.1",
    "terser-webpack-plugin": "^5.3.9",
    "copy-webpack-plugin": "^11.0.0"
  }
}